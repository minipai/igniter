{
  "name": "@minipai/igniter",
  "version": "0.4.0",
  "description": "Coordinate Linear tickets and coding agents through a Bun CLI and Herdr.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/minipai/igniter.git"
  },
  "homepage": "https://github.com/minipai/igniter#readme",
  "bugs": {
    "url": "https://github.com/minipai/igniter/issues"
  },
  "type": "module",
  "engines": {
    "bun": ">=1.3.11"
  },
  "bin": {
    "igniter": "./src/cli.ts"
  },
  "files": [
    "src/",
    "!src/**/*.test.ts",
    "!src/e2e/",
    "!src/**/fake-*.ts",
    "!src/herdr/generate-types.ts"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "generate:herdr": "bun src/herdr/generate-types.ts",
    "test": "bun test src/package.test.ts src/cli.test.ts src/herdr src/workflow src/commander scripts/release.test.ts",
    "test:e2e": "bun src/e2e/support/run.ts",
    "check": "bun run typecheck && bun run test && bun run test:e2e",
    "release": "bun scripts/release.ts",
    "release:publish": "bun scripts/release.ts --publish"
  },
  "dependencies": {
    "cac": "7.0.0"
  },
  "devDependencies": {
    "@types/node": "26.4.1",
    "bun-types": "1.4.0",
    "typescript": "7.0.2"
  }
}
